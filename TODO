- OpenRewrite error check + Style change detection 
- Current style change working on commit level > Why not (commit, file) level?
- Add empty corpus to document or not? (Affects score)
- Apply voting on bug2commit like fonte?
- Make them all work on pid, vid set